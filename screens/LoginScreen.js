import React from 'react';
import { View, Text } from 'react-native';

const LoginScreen = ({ navigation }) => {
  return (
    <View>
      <Text>Login Screen</Text>
      {/* Diğer bileşenler ve işlemler */}
    </View>
  );
};

export default LoginScreen;
